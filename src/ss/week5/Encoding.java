package ss.week5;

import java.util.Base64;

/*
import org.apache.commons.codec.DecoderException;
import org.apache.commons.codec.binary.Base64;
import org.apache.commons.codec.binary.Hex;

/**
 * A simple class that experiments with the Hex encoding
 * of the Apache Commons Codec library.

public class Encoding {
    public static void main(String[] args) throws DecoderException {
        String input = "Hello World";
        System.out.println("Module 2");
        //System.out.println(Hex.encodeHexString(input.getBytes()));
        base64Encode();
    }

    public static void base64Encode() throws DecoderException{
        String helloWorld = Base64.encodeBase64String("Hello World".getBytes());
        byte[] decHex = Hex.decodeHex("010203040506");
        String baseString = Base64.encodeBase64String(decHex);
        System.out.println(baseString);

        byte[] decBase = Base64.decodeBase64("U29mdHdhcmUgRGV2ZWxvcG1lbnQ");
        System.out.println(decBase);

        String a = "";
        for (int i = 0; i <= 9; i++) {
            a+= "a";
            System.out.println(a.getBytes());

        }

    }

}
*/
